name: MATCH AI - V4 Research Lab
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/match-v4-research.yml"
  schedule:
    - cron: "43 * * * *"
permissions:
  contents: write
concurrency:
  group: match-ai-state-writer
  cancel-in-progress: false
jobs:
  research:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install websockets==13.1
      - name: Refresh audited ticks
        run: python collect_ticks.py
      - name: Run V4 model tournament
        run: python research_v4.py
      - name: Persist research
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add audit.json ticks.json memory/
          git diff --cached --quiet || git commit -m "V4 autonomous research"
          for attempt in 1 2 3; do
            git pull --rebase origin main
            if git push; then
              break
            fi
            if [ "$attempt" -eq 3 ]; then
              exit 1
            fi
            sleep 3
          done